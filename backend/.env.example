# Configuración del servidor
PORT=3000
NODE_ENV=development

# Base de datos MongoDB
MONGODB_URI=mongodb://localhost:27017/utpl_herbario

# JWT Configuración
JWT_SECRET=tu_clave_secreta_super_segura_aqui_cambiar_en_produccion
JWT_EXPIRES_IN=7d

# URL base para códigos QR públicos
PUBLIC_BASE_URL=http://localhost:3000

# Configuración bcrypt
BCRYPT_SALT_ROUNDS=12

# Configuración de imágenes
# Opciones: 'file' (guardar en disco) o 'base64' (guardar en MongoDB)
IMAGE_STORAGE_MODE=file
